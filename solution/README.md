# GCC as a Service - Solution

1. Use `--help.c` or `--version.c` as a filename.
2. Then replace `system("echo-flag | cut -c1-10");` by `system("echo-flag");` in the sample code.
3. Hit the **Compile & Run** button.
4. And that's it.